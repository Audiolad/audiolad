/**
 * Slot for future real counts, client logos, and reviews.
 * Renders nothing until that data exists. Do not pass placeholders.
 */
export default function BusinessSocialProofSlot() {
  return null;
}
