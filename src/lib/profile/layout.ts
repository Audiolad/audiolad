/** Profile content padding inside listener shell center column. */
export const profilePagePaddingClassName = "px-5 pt-6 pb-8 xl:px-8 xl:pt-8";

/**
 * Two-column grid from xl: aligns with listener desktop shell breakpoint.
 * DOM order matches mobile (user → continue → …).
 */
export const profilePageGridClassName =
  "grid grid-cols-1 items-start xl:grid-cols-[minmax(320px,2fr)_minmax(0,3fr)] xl:gap-x-6";

export const profilePageFullWidthClassName = "col-span-full min-w-0";

export const profilePageLeftColumnClassName = "min-w-0";

/** Edit profile form padding inside listener shell. */
export const profileEditPaddingClassName =
  "px-5 pt-5 pb-10 xl:px-8 xl:pt-6 xl:pb-8";
